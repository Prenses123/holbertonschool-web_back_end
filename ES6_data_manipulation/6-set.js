/**
 * Creates a Set containing unique elements from the provided array.
 * @param {Array} array - The source array of elements.
 * @returns {Set} A new Set containing the unique values from the array.
 */
const setFromArray = (array) => {
    return new Set(array);
  };
  
  export default setFromArray;
